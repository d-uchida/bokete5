<h2>ボケてアカウント新規登録</h2>
<div class="">
  ボケてでは色々な人に「ボケて」もらって「評価」してもらいたいと思っています。投稿したボケが、誰かの辛い時間を楽しい時間に変えることもできるし、面白い！と思うボケにコメントしたことがボケ投稿者をハッピーにすることが出来ます。<br>
  ボケては完全に無料で利用でき、アカウントを作成すればPCサイトだけでなくスマホサイトやiOSアプリ、Androidアプリでも楽しめます。ボケてに登録してみんなを楽しい気持ちにしてください。
</div>




<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  <div class="field">
    <%= f.label :メールアドレス %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>
  <div class="field">
    <%= f.label :ボケてID（２文字以上１５文字以内、半角英数字またはアンダースコア） %><br />
    <%= f.text_field :account_name, autofocus: true %>
  </div>
  <div class="field">
    <%= f.label :パスワード（確認） %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>
  <div class="field">
    <%= f.label :ニックネーム（10文字以内） %><br />
    <%= f.text_field :nickname, autofocus: true %>
  </div>
  <div class="field">
    <%= f.label :性別 %><br />
    <%= f.select(:sex, {"男": "male", "女": "female" }, include_blank: true) %>
  </div>
  <div class="field">
    <%= f.label :都道府県 %><br />
  </div>

  <div class="field">
    <%= f.label :生年月日（オプション） %><br />
    <%= f.date_select(
      :birthday,
      start_year: Time.now.year,
      end_year: 1900,
      use_month_numbers: true,
      include_blank: true
      ) %>
  </div>
  <div class="field">
    <%= f.label :プロフィール %><br />
    <%= f.text_field :nickname, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "送信", render: 'new' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
